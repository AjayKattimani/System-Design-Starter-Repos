{
  "name": "fireline-retrieval-repair",
  "version": "1.0.0",
  "description": "LU 2.11 Assignment: Fix the broken Fireline retrieval (pgvector) schema to match the retrieval spec",
  "scripts": {
    "db:reset": "psql -c 'DROP DATABASE IF EXISTS fireline_test;' && psql -c 'CREATE DATABASE fireline_test;' && psql -d fireline_test -c 'CREATE EXTENSION IF NOT EXISTS vector; CREATE EXTENSION IF NOT EXISTS pgtap;' && psql -d fireline_test -f migrations/001_retrieval_schema.sql",
    "test": "pg_prove -d fireline_test tests/schema.test.sql"
  },
  "devDependencies": {}
}
